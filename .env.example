# PostgreSQL connection string used by Prisma.
# Local dev: point this at a local/dev Postgres instance (e.g. Docker, Neon, Supabase).
# On Render, this is provided automatically by the linked database in render.yaml.
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"
